// Enum for User Roles
const Roles = {
  ADMIN: 'admin',
  MENTEE: 'mentee',
};

// Enum for Hour Request Status
const Status = {
  PENDING: 'pending',
  APPROVED: 'approved',
  REJECTED: 'rejected',
};
  
module.exports = {
  Roles,
  Status,
};